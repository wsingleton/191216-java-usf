spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://localhost:8888

# this is how you comment in a yml file:
# spring.application.name=discovery-service
# spring.cloud.config.uri=http://localhost:8888
